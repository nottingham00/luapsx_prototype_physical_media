Place a PS5-built libretro PS1 core here as:

  pcsx_rearmed_libretro.so

At runtime the native LuaPSX payload stages this directory to:

  /data/luapsx/cores/

The prototype does not redistribute PCSX-ReARMed. Build the core from its
upstream GPLv2 source for your own PS5 homebrew environment.
